apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-service
  namespace: kraivor-prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: core-service
  template:
    metadata:
      labels:
        app: core-service
    spec:
      containers:
      - name: core
        image: ${ECR_REGISTRY}/kraivor-core:latest
        ports:
        - containerPort: 8002
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: kraivor-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: kraivor-secrets
              key: redis-url
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: core-service
  namespace: kraivor-prod
spec:
  selector:
    app: core-service
  ports:
  - port: 8002
    targetPort: 8002
  type: ClusterIP
