name: PR Title Check

on:
  pull_request:
    branches: [dev]
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate conventional title
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            chore
            ci
            security
          requireScope: false
          subjectPattern: ^[a-zA-Z].+$
          subjectPatternError: |
            The subject "{subject}" must start with a letter (lowercase or uppercase) and cannot start with a number.
          validateSingleCommit: false
          wip: false
